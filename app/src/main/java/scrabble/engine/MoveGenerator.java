package scrabble.engine;

import scrabble.core.Move;
import scrabble.core.GameState;
import scrabble.rules.AdvancedDictionary;

import java.util.List;

public class MoveGenerator {
    public static List<Move> legalMoves(GameState gameState, AdvancedDictionary dictionary) {
        // TO-DO: Generate all possible legal moves
        return null;
    }
}
